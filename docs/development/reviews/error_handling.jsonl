{"id": "REV-error_handling-001", "severity": "HIGH", "category": "bare-except-swallows-errors", "file": "src/camtasia/project.py", "lines": "79-80", "title": "_probe_media swallows all pymediainfo exceptions", "description": "The `except Exception: pass` on line 79 catches every non-ImportError from pymediainfo (RuntimeError, OSError, AttributeError, etc.) and silently falls through to ffprobe. Callers never learn that pymediainfo was available but failed. A corrupt file that pymediainfo could diagnose gets silently downgraded to ffprobe's best-effort output with no warning.", "suggestion": "Catch specific expected exceptions (e.g. `RuntimeError`, `OSError`) and log a `warnings.warn()` for unexpected ones before falling through. At minimum, log the exception type so users can diagnose probe failures.", "repro": "docs/development/reviews/repros/REV-error_handling-001.py", "verified": false, "verification_status": "PENDING"}
{"id": "REV-error_handling-002", "severity": "HIGH", "category": "bare-except-swallows-errors", "file": "src/camtasia/project.py", "lines": "97-98, 108-109, 131-132", "title": "_probe_media_ffprobe swallows all exceptions in three subprocess blocks", "description": "Each of the three ffprobe subprocess calls uses `except Exception: pass`. This catches PermissionError, MemoryError, and programming bugs (AttributeError, TypeError) alongside expected FileNotFoundError and TimeoutExpired. The function returns `{'_backend': 'ffprobe'}` with no metadata and no indication of failure. Callers then use wrong defaults (e.g. 1920x1080 for audio, 1-second duration for video).", "suggestion": "Catch `(FileNotFoundError, subprocess.TimeoutExpired, subprocess.CalledProcessError, OSError, ValueError)` explicitly, matching the pattern already used in `_detect_codec` at line 637. Log unexpected exceptions via `warnings.warn`.", "repro": "docs/development/reviews/repros/REV-error_handling-002.py", "verified": false, "verification_status": "PENDING"}
{"id": "REV-error_handling-003", "severity": "HIGH", "category": "bare-except-swallows-errors", "file": "src/camtasia/media_bin/media_bin.py", "lines": "700-701", "title": "_parse_with_pymediainfo swallows all exceptions from MediaInfo.parse", "description": "The `except Exception: return None` catches everything from pymediainfo including OSError (broken libmediainfo), AttributeError (API changes), and MemoryError. When this returns None, import_media raises `ValueError('Cannot determine media type')` — an unactionable message that hides the real cause (e.g. broken native library).", "suggestion": "Catch `(RuntimeError, OSError)` for expected pymediainfo failures. Re-raise or warn on unexpected exceptions so the real cause is visible.", "repro": "docs/development/reviews/repros/REV-error_handling-003.py", "verified": false, "verification_status": "PENDING"}
{"id": "REV-error_handling-004", "severity": "MEDIUM", "category": "unactionable-error-message", "file": "src/camtasia/timeline/track.py", "lines": "536", "title": "Warning for None source_id doesn't guide user to fix", "description": "When `source_id is None` for a media file clip, the code warns `'source_id is None for media file clip; using src=0'` and silently assigns `src=0`. This creates a clip pointing to media ID 0, which may not exist or may be the wrong media. The warning doesn't tell the user what to do about it or why source_id was None.", "suggestion": "Either raise ValueError (since a media file clip without a source is likely a bug) or include guidance in the warning: which clip type, what the caller should pass, and that src=0 is a fallback that may reference wrong media.", "verified": false, "verification_status": "PENDING"}
{"id": "REV-error_handling-005", "severity": "HIGH", "category": "unactionable-error-message", "file": "src/camtasia/operations/media_ops.py", "lines": "70", "title": "remove_media error message lacks context about which references exist", "description": "When `clear_tracks=False` and track references exist, the error is: `ValueError('Attempt to remove media from media-bin while references exist on tracks')`. This doesn't identify the media_id, which tracks, or which clip IDs hold references. Users must manually search the entire timeline to find the references.", "suggestion": "Collect the referencing track names and clip IDs before raising: `raise ValueError(f'Cannot remove media {media_id}: referenced by clips {refs} on tracks {track_names}')`.", "repro": "docs/development/reviews/repros/REV-error_handling-005.py", "verified": false, "verification_status": "PENDING"}
{"id": "REV-error_handling-006", "severity": "MEDIUM", "category": "silent-failure", "file": "src/camtasia/project.py", "lines": "1728-1733", "title": "add_voiceover_sequence silently falls back to 1.0s duration on probe failure", "description": "When `_probe_media` returns no `duration_seconds`, the code warns and falls back to `dur = 1.0`. While the warning is present (good), the resulting clip has wrong timing. For a batch of voiceover files, this silently produces a broken timeline where all unprobed clips are 1 second. The warning is easy to miss in programmatic usage.", "suggestion": "Consider raising an error by default with an opt-in `fallback_duration` parameter. Users who want the 1.0s fallback can explicitly pass it. This follows the principle of 'errors should not pass silently'.", "verified": false, "verification_status": "PENDING"}
{"id": "REV-error_handling-007", "severity": "MEDIUM", "category": "missing-input-validation", "file": "src/camtasia/operations/merge.py", "lines": "89-130", "title": "merge_tracks does not validate offset_seconds is non-negative", "description": "The `merge_tracks` function accepts `offset_seconds` without validation. A negative value produces negative `start` ticks on clips, which is invalid in Camtasia's format and will cause rendering failures or data corruption. Other layout functions (e.g. `layout.py:31`) validate non-negative values.", "suggestion": "Add `if offset_seconds < 0: raise ValueError(f'offset_seconds must be non-negative, got {offset_seconds}')` at the start of the function, consistent with the pattern in `operations/layout.py`.", "verified": false, "verification_status": "PENDING"}
{"id": "REV-error_handling-008", "severity": "LOW", "category": "silent-failure", "file": "src/camtasia/export/edl.py", "lines": "85-86", "title": "EDL export silently uses 'AX' placeholder when media lookup fails", "description": "When `project.media_bin[src_id]` raises KeyError, the except block silently falls through and uses `source = 'AX'` (the default). The exported EDL contains a generic placeholder instead of the real source name, with no warning to the user that media resolution failed.", "suggestion": "Emit a `warnings.warn(f'Media {src_id} not found in source bin; using placeholder in EDL', stacklevel=2)` so users know the EDL is incomplete.", "verified": false, "verification_status": "PENDING"}
{"id": "REV-error_handling-009", "severity": "LOW", "category": "wrong-exception-type", "file": "src/camtasia/timeline/markers.py", "lines": "122, 135, 148", "title": "MarkerList uses ValueError for lookup failures instead of KeyError", "description": "Three methods (`rename_marker`, `move_marker`, `remove_marker_by_name`) raise `ValueError` when a marker is not found by name or time. However, these are lookup failures — the standard Python convention is `KeyError` for 'item not found in collection'. The `remove_marker_at` method on line 106 correctly uses `KeyError`.", "suggestion": "Change to `KeyError` for consistency with `remove_marker_at` and Python collection conventions. This is a breaking change for callers catching ValueError, so consider a deprecation period.", "verified": false, "verification_status": "PENDING"}
{"id": "REV-error_handling-010", "severity": "MEDIUM", "category": "missing-input-validation", "file": "src/camtasia/operations/batch.py", "lines": "16-22", "title": "apply_to_clips does not handle exceptions from user-provided callback", "description": "The `apply_to_clips` function calls `fn(clip)` for each clip with no error handling. If `fn` raises on the 5th of 10 clips, the first 4 clips are already mutated and the remaining 5 are untouched, leaving the timeline in a partially-modified state. This is especially dangerous for `apply_to_all_tracks` which spans the entire timeline.", "suggestion": "Document the partial-application behavior explicitly, or add a `rollback_on_error` option. At minimum, catch exceptions and report which clip failed: `raise RuntimeError(f'Callback failed on clip {clip.id}: {e}') from e`.", "verified": false, "verification_status": "PENDING"}
{"id": "REV-error_handling-011", "severity": "LOW", "category": "silent-failure", "file": "src/camtasia/builders/screenplay_builder.py", "lines": "209-210", "title": "_find_audio_for_vo returns None on OSError without logging", "description": "When `audio_dir.iterdir()` raises OSError (e.g. permission denied, broken symlink), the function catches it and returns None. The caller in `build_from_screenplay` then warns 'No audio file found for VO block' — a misleading message since the real problem is directory access failure, not a missing file.", "suggestion": "Log the OSError in the except block: `warnings.warn(f'Cannot read audio directory {audio_dir}: {e}', stacklevel=2)` so the real cause is visible.", "verified": false, "verification_status": "PENDING"}
{"id": "REV-error_handling-012", "severity": "MEDIUM", "category": "missing-input-validation", "file": "src/camtasia/export/audio.py", "lines": "82-110", "title": "export_audio does not validate solo_track exists", "description": "When `solo_track` is set to a non-existent track name, `export_audio` silently produces an empty CSV/JSON file with no warning. The user gets a valid but empty output file and may not realize the track name was wrong.", "suggestion": "After collecting clips, if `solo_track is not None` and `rows` is empty, emit `warnings.warn(f'No audio clips found on track {solo_track!r}; output file is empty')` or raise KeyError if the track doesn't exist at all.", "verified": false, "verification_status": "PENDING"}
{"id": "REV-error_handling-013", "severity": "LOW", "category": "lost-exception-chain", "file": "src/camtasia/builders/slide_import.py", "lines": "36", "title": "ImportError re-raised with 'from None' hides original traceback", "description": "The `raise ImportError(...) from None` on line 36 suppresses the original ImportError traceback. While the custom message is helpful, the original exception may contain useful context (e.g. which specific DLL or .so file was missing, or a version conflict).", "suggestion": "Use `from exc` instead of `from None` to preserve the original traceback: `except ImportError as exc: raise ImportError(...) from exc`.", "verified": false, "verification_status": "PENDING"}
{"id": "REV-error_handling-014", "severity": "MEDIUM", "category": "missing-input-validation", "file": "src/camtasia/library/libzip.py", "lines": "55-76", "title": "import_libzip does not validate manifest.json structure", "description": "The `import_libzip` function reads `manifest.json` and accesses `manifest_data.get('assets', [])` and iterates entries expecting `entry['file']` and `asset_data['name']`. If the manifest is malformed (missing keys, wrong types), the user gets an unhelpful KeyError or TypeError from deep in the zip-reading loop instead of a clear validation error.", "suggestion": "Validate the manifest structure after loading: check that 'assets' is a list, each entry has 'file', and each asset file has 'name' and 'kind'. Raise `ValueError('Malformed libzip manifest: ...')` with specifics.", "verified": false, "verification_status": "PENDING"}
{"id": "REV-error_handling-015", "severity": "LOW", "category": "silent-failure", "file": "src/camtasia/timeline/clips/group.py", "lines": "130-134", "title": "GroupTrack.add_clip warns about auto-generated ID but proceeds anyway", "description": "When `next_id` is None, the code auto-generates a locally-unique ID and emits a warning about global uniqueness. However, the warning is easy to miss and the resulting clip may have an ID that collides with clips on other tracks, causing data corruption during save/load.", "suggestion": "Consider raising ValueError when next_id is None, forcing callers to provide a globally-unique ID. The warning-and-proceed pattern creates a latent bug that only manifests when the project is reloaded.", "verified": false, "verification_status": "PENDING"}
{"id": "REV-error_handling-016", "severity": "LOW", "category": "unactionable-error-message", "file": "src/camtasia/timeline/clips/unified.py", "lines": "46", "title": "AttributeError message for missing audio child doesn't suggest alternatives", "description": "When accessing `.audio` on a UnifiedMedia without an audio child, the error is: `'UnifiedMedia has no audio child; check .has_audio first'`. While it mentions `.has_audio`, it doesn't explain what to do if the user needs audio — e.g. use a separate AMFile clip or add an audio source.", "suggestion": "Expand the message: `'UnifiedMedia has no audio child. Check .has_audio before accessing. To add audio, use track.add_audio() as a separate clip.'`", "verified": false, "verification_status": "PENDING"}
